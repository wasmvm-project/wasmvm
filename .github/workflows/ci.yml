name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run automated tests
      run: |
        npx tsx scripts/test-vfs-shell.ts
        npx tsx scripts/test-wasi-runner.ts
        npx tsx scripts/test-all-features.ts

    - name: Build Next.js application
      run: npm run build
